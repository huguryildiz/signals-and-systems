---
paths:
  - "source/**/*"
  - "build/src/8*.js"
  - "build/src/9*.js"
  - "notes/src/**/*.js"
---

# Source evidence and corrections

Never author material from source pp. 22–88 using a second-hand inventory. Read the pages at 160 dpi first (`pdftoppm -r 160 -png`). Known possible source errors include analysis/synthesis label swaps, sign errors, and a 1000× sampling-period slip; verify each before treating it as an error.

Never correct the source silently. Record a confirmed issue in the ledger format of `.claude/reports/Phase1_Report.md` §2 and state the correction in the artifact where it occurs. Consult `.claude/reports/` when the source is in doubt; continue ambiguity identifiers from A-09 as directed by the current ledger. `Book.pdf` is for cross-checking only and must not be redistributed.
