/* Question bank, Module 7 — Sampling and Aliasing */
CONTENT.QBANK = CONTENT.QBANK.concat([

{ id:'Q7-01', module:'M7', kind:'concept', src:'pp. 80–81',
  stem:'A signal $x(t)$ is multiplied by an impulse train $p(t)=\\sum_n\\delta(t-nT)$. Which statement about the result is correct?',
  opts:['$x_p(t)=\\sum_n x(nT)\\delta(t-nT)$ and $X_p(j\\omega)=\\frac{1}{T}\\sum_k X\\bigl(j(\\omega-k\\omega_s)\\bigr)$, and both hold for every value of $T$.',
        '$x_p(t)=\\sum_n x(nT)\\delta(t-nT)$ and $X_p(j\\omega)=\\sum_k X\\bigl(j(\\omega-k\\omega_s)\\bigr)$, with no factor in front of the sum.',
        '$X_p(j\\omega)=X(j\\omega)P(j\\omega)$, because multiplication in time is multiplication in frequency.',
        'The copies appear in $X_p(j\\omega)$ only when $\\omega_s>2\\omega_M$; below that rate the spectrum is no longer replicated.'],
  a:0,
  why:'The sifting property turns the product into $\\sum_n x(nT)\\delta(t-nT)$. In frequency, multiplication becomes convolution divided by $2\\pi$, and the transform of the impulse train is $\\frac{2\\pi}{T}\\sum_k\\delta(\\omega-k\\omega_s)$, so the two factors of $2\\pi$ leave $1/T$ in front. No step in that derivation uses the value of $T$.',
  wrong:{1:'The time-domain half is right and the frequency-domain half has lost its scale factor. Every copy is $X(j\\omega)$ divided by $T$, and the reconstruction filter carries the matching gain $T$. A missing $1/T$ gives an answer wrong by a factor that changes whenever the rate changes.',
         2:'Multiplication in time is convolution in frequency, not multiplication: $X_p=\\frac{1}{2\\pi}[X*P]$. Multiplying the two transforms would describe convolving the two signals in time, which is a different operation entirely.',
         3:'The formula for $X_p$ has no condition attached to it, so the copies are present at every rate. What changes below $\\omega_s=2\\omega_M$ is that neighbouring copies overlap and add; they do not fail to appear.'},
  hints:['Apply the sifting property $x(t)\\delta(t-nT)=x(nT)\\delta(t-nT)$ term by term.',
         'In frequency, ask what multiplication in time becomes, and what the transform of an impulse train of period $T$ is.'],
  sol:'Time domain, by sifting:$$x_p(t)=x(t)\\sum_{n}\\delta(t-nT)=\\sum_{n}x(nT)\\,\\delta(t-nT).$$Frequency domain, in three steps. Multiplication in time is convolution in frequency with a factor $1/2\\pi$:$$X_p(j\\omega)=\\frac{1}{2\\pi}\\bigl[X(j\\omega)*P(j\\omega)\\bigr].$$The impulse train transforms to an impulse train,$$P(j\\omega)=\\frac{2\\pi}{T}\\sum_{k}\\delta(\\omega-k\\omega_s),\\qquad \\omega_s=\\frac{2\\pi}{T},$$and convolving with a shifted impulse shifts the function. Putting the three together:$$X_p(j\\omega)=\\frac{1}{T}\\sum_{k=-\\infty}^{\\infty}X\\bigl(j(\\omega-k\\omega_s)\\bigr).$$Result: a copy of $X(j\\omega)$ at every multiple of $\\omega_s$, each scaled by $1/T$.<br>Check: nothing in the derivation restricted $T$, so the statement is unconditional. Whether the copies overlap is a separate question, decided by $\\omega_s-2\\omega_M$.',
  err:'Attaching a condition to the replication itself, so that the copies are believed to appear only at high rates and to vanish at low ones.',
  teach:'Ask the student to state, in one sentence each, what is true at every rate and what is true only above the Nyquist rate. If both sentences mention the copies appearing, the distinction has not landed.' },

{ id:'Q7-02', module:'M7', kind:'concept', src:'p. 88',
  stem:'A signal is not band-limited and must be sampled at a fixed rate $\\omega_s$. Where does the anti-aliasing filter belong, and why?',
  opts:['Before the sampler, because once the high frequencies have folded into the band they have been added to the wanted content and cannot be separated from it.',
        'After the sampler, because that is where the unwanted frequencies appear, so that is where they should be removed.',
        'Either side, because a lowpass filter with cutoff $\\omega_s/2$ removes the same frequencies wherever it is placed in the chain.',
        'Neither: an anti-aliasing filter is a reconstruction filter, and one filter with gain $T$ at the output does both jobs.'],
  a:0,
  why:'The sampler adds the copies together. After that, a single value of $X_p(j\\omega)$ inside the band is the sum of a wanted contribution and an intruding one, and a filter can only select intervals of $\\omega$, not undo an addition. Removing the offending content while it is still at its own frequency is the only point at which it can be removed cleanly.',
  wrong:{1:'By then the damage is done. Below the Nyquist rate the high frequencies do not stay where they were; they land inside the band and add to what was already there. A filter downstream removes both contributions or neither.',
         2:'The two placements are not equivalent. Upstream of the sampler the unwanted content sits above $\\omega_s/2$ and can be cut away; downstream it has been folded to a position below $\\omega_s/2$, on top of the signal.',
         3:'They are different filters doing different jobs at different points. The anti-aliasing filter limits the band of the signal before it is sampled; the reconstruction filter selects the baseband from the copies afterwards. A chain with only the second one still aliases.'},
  hints:['Write down what the sampler does to a frequency component sitting above $\\omega_s/2$.',
         'Ask whether any filter can recover two numbers from their sum.'],
  sol:'Take a component at $\\omega_1$ with $\\omega_s/2<\\omega_1<\\omega_s$. After sampling, the copy at $k=-1$ puts a contribution at $\\omega_1-\\omega_s$, whose magnitude is below $\\omega_s/2$. It now sits inside the band the reconstruction filter keeps, and it has been added to whatever the signal already had at that frequency.<br>A filter is a multiplication in the frequency domain: it can scale $X_p(j\\omega)$ at each $\\omega$, including scaling it to zero. It cannot look at one value and report the two numbers that were summed to make it. So the intruder is removable only while it is still at $\\omega_1$, which is before the sampler.<br>Result: the chain is $x(t)\\to H_{AA}(j\\omega)\\to\\text{sampler}\\to H_r(j\\omega)\\to x_r(t)$.<br>Check by cost: the filter throws away the content above $\\omega_s/2$, which was going to be lost either way. What it prevents is the appearance of content at frequencies the signal never contained, and that is the part nothing downstream can detect.',
  err:'Treating aliasing as noise that shows up at the output and can therefore be filtered at the output.',
  teach:'Draw the chain twice on the board, with the filter in each position, and follow one component at $0.7\\omega_s$ through both. The difference is visible in two steps.' },

{ id:'Q7-03', module:'M7', kind:'concept', src:'pp. 83–85',
  stem:'A converter reconstructs a signal with a zero-order hold, sampling well above the Nyquist rate. Which statement is correct?',
  opts:['The output is an approximation: $H_0(j\\omega)=e^{-j\\omega T/2}\\,2\\sin(\\omega T/2)/\\omega$ is not flat inside the band and is not zero outside it, so the output differs from $x(t)$ by an amount that can be computed.',
        'The output equals $x(t)$, because the sampling theorem is satisfied and the theorem guarantees exact recovery.',
        'The output equals $x(t)$ apart from a delay of $T/2$, which is the only effect the hold has.',
        'The hold is exact provided the rate is raised far enough, because the steps become arbitrarily small.'],
  a:0,
  why:'The theorem guarantees that the samples <em>determine</em> $x(t)$, and it names the filter that recovers it: gain $T$, cutoff inside the guard band, which is an ideal lowpass. A hold is a different filter. Its magnitude sags across the band and leaks beyond it, so the staircase is a different signal from $x(t)$.',
  wrong:{1:'The theorem says the samples are sufficient and states which filter extracts the signal from them. Satisfying the rate condition does not make every filter the right one; feeding the same samples to a hold gives a different output.',
         2:'The linear phase $-\\omega T/2$ is indeed a delay of half a period, but it is not the only effect. The magnitude $|2\\sin(\\omega T/2)/\\omega|$ falls away from $T$ as $\\omega$ grows and is non-zero beyond $\\omega_s$, so the output is shaped as well as delayed.',
         3:'Raising the rate makes the error smaller, but it never makes it zero at any finite rate. The staircase always has jumps, and $x(t)$ does not.'},
  hints:['Write down the impulse response of the hold and transform it.',
         'Compare that response with the one the sampling theorem asks for: constant $T$ inside the band, exactly zero outside.'],
  sol:'Impulse response of the zero-order hold:$$h_0(t)=\\begin{cases}1,&0\\le t<T\\\\0,&\\text{otherwise.}\\end{cases}$$Transform:$$H_0(j\\omega)=\\int_0^T e^{-j\\omega t}\\,dt=\\frac{1-e^{-j\\omega T}}{j\\omega}=e^{-j\\omega T/2}\\,\\frac{2\\sin(\\omega T/2)}{\\omega}.$$Compare with the ideal reconstruction filter, which is exactly $T$ on $|\\omega|<\\omega_c$ and exactly zero outside. Three differences: $H_0(0)=T$ but the magnitude sags below $T$ as $\\omega$ rises; the magnitude is not zero beyond $\\omega_c$, so parts of the neighbouring copies survive; and the phase is $-\\omega T/2$, a delay of half a period.<br>Making the hold exact needs the compensator$$H_r(j\\omega)=\\frac{H(j\\omega)}{H_0(j\\omega)}=e^{j\\omega T/2}H(j\\omega)\\,\\frac{\\omega}{2\\sin(\\omega T/2)},$$which has to advance in time and to grow without bound where $\\sin(\\omega T/2)$ vanishes. It is approximated in practice, not built.<br>Result: the hold output is an approximation with a computable error, at any rate.',
  err:'Reading the sampling theorem as a guarantee about the hardware rather than as a statement about the samples and one particular filter.',
  teach:'Ask for the error to be quantified rather than described. A number makes the point that a description does not.' },

{ id:'Q7-04', module:'M7', kind:'calc', src:'p. 83',
  stem:'A signal is sampled at $\\omega_s=8000\\pi$ rad/s. What is the sampling period $T$?',
  opts:['$T=2.5\\times10^{-4}$ s, that is $0.25$ ms.',
        '$T=0.25$ s.',
        '$T=1.25\\times10^{-4}$ s, that is $125\\ \\mu$s.',
        '$T=3.98\\times10^{-5}$ s.'],
  a:0,
  why:'$T=2\\pi/\\omega_s=2\\pi/(8000\\pi)=1/4000$ s $=2.5\\times10^{-4}$ s. The check $\\omega_sT=8000\\pi\\times2.5\\times10^{-4}=2\\pi$ confirms it in one multiplication.',
  wrong:{1:'This is the same arithmetic with the factor of a thousand left uncancelled. It fails the check at once: $8000\\pi\\times0.25=2000\\pi$, not $2\\pi$. It would also mean four samples per second for a signal carrying 2000 Hz.',
         2:'That is the period for $\\omega_s=16000\\pi$ rad/s, twice the rate asked about. Halving the rate must double the period, so this answer is a factor of two too small.',
         3:'This is $1/\\omega_s$ rather than $2\\pi/\\omega_s$: the conversion from an angular frequency to a period has been made without the $2\\pi$. The unit test catches it: $8000\\pi\\times3.98\\times10^{-5}=1$, not $2\\pi$.'},
  hints:['A rate given in rad/s and a period are linked by $\\omega_sT=2\\pi$, not by $\\omega_sT=1$.',
         'Before writing the answer down, multiply it by the rate and see whether $2\\pi$ comes back.'],
  sol:'The rate is angular, so the conversion carries $2\\pi$:$$T=\\frac{2\\pi}{\\omega_s}=\\frac{2\\pi}{8000\\pi}=\\frac{1}{4000}\\ \\text{s}=2.5\\times10^{-4}\\ \\text{s}=0.25\\ \\text{ms}.$$The matching frequency in hertz is $f_s=1/T=4000$ Hz, and indeed $\\omega_s=2\\pi f_s=8000\\pi$ rad/s.<br>Three checks, each independent of the others.<br>1. $\\omega_sT=2\\pi$: $8000\\pi\\times2.5\\times10^{-4}=2\\pi$.<br>2. The copies of the spectrum are scaled by $1/T=4000$. A period of $0.25$ s would scale them by 4.<br>3. Doubling the rate must halve the period: at $\\omega_s=16000\\pi$ the period is $125\\ \\mu$s, and $0.25$ ms divided by $125\\ \\mu$s is exactly 2.<br>Result: $T=0.25$ ms.',
  err:'Cancelling the $\\pi$ but not the thousand, so a period a thousand times too long is carried through every later part of the problem.',
  teach:'Insist on the unit check as part of the answer, not as an optional extra. It is one multiplication and it catches three separate classes of slip.' },

{ id:'Q7-05', module:'M7', kind:'calc', src:'p. 83',
  stem:'A signal has a triangular spectrum whose peak is $X_{\\max}=X(0)=4000$, produced by convolving two rectangles whose overlap at zero shift has area $A=8000\\pi$. It is sampled with $T=1.25\\times10^{-4}$ s. How tall is each copy in $X_p(j\\omega)$?',
  opts:['$X_{\\max}/T=4000\\times8000=3.2\\times10^{7}$.',
        '$A/T=8000\\pi\\times8000\\approx2.01\\times10^{8}$.',
        '$X_{\\max}\\cdot T=4000\\times1.25\\times10^{-4}=0.5$.',
        '$X_{\\max}=4000$, unchanged, because sampling shifts the copies without rescaling them.'],
  a:0,
  why:'Each copy is $X(j\\omega)$ scaled by $1/T$, so the peak of a copy is the peak of the spectrum divided by $T$. The peak is $X_{\\max}=A/2\\pi=4000$, and $1/T=8000$, giving $3.2\\times10^{7}$.',
  wrong:{1:'$A$ is the area of the overlap of the two rectangles, not the peak of the spectrum. The two differ by $2\\pi$, because squaring in time convolves in frequency and divides by $2\\pi$: $X_{\\max}=A/2\\pi=4000$. Using $A$ where $X_{\\max}$ belongs makes every height $2\\pi$ times too large.',
         2:'The factor is $1/T$, not $T$. Multiplying by a period of $1.25\\times10^{-4}$ s makes the copies smaller than the original by four orders of magnitude, when sampling faster is what makes them taller.',
         3:'Sampling both shifts and rescales. The scale factor $1/T$ is what the reconstruction filter\u2019s gain $T$ is there to undo, so dropping one without dropping the other leaves the recovered signal the wrong size.'},
  hints:['Separate the two quantities first: which number is an area and which is the peak of $X(j\\omega)$?',
         'Then apply the scale factor that appears in front of the sum in $X_p(j\\omega)$.'],
  sol:'Two different quantities carry the word size here, and $2\\pi$ separates them.$$A=\\bigl[R*R\\bigr](0)=\\int_{-4000\\pi}^{4000\\pi}(1)(1)\\,d\\tau=8000\\pi,\\qquad X_{\\max}=X(0)=\\frac{A}{2\\pi}=4000.$$Sampling gives$$X_p(j\\omega)=\\frac{1}{T}\\sum_k X\\bigl(j(\\omega-k\\omega_s)\\bigr),$$so the peak of every copy is $X_{\\max}/T$. With $T=1.25\\times10^{-4}$ s, $1/T=8000$, and$$X_{\\max}/T=4000\\times8000=3.2\\times10^{7}.$$Check on the rate: $\\omega_s=2\\pi/T=16000\\pi$ rad/s, which is exactly twice $\\omega_M=8000\\pi$, so this is the Nyquist rate and the copies just touch.<br>Result: $3.2\\times10^{7}$.',
  err:'Substituting the area $A$ wherever the symbol appears, so the peak of the spectrum and the area under a convolution are used interchangeably.',
  teach:'Have the two quantities named separately on the board before any number is substituted. The slip disappears once they have different symbols.' },

{ id:'Q7-06', module:'M7', kind:'calc', src:'p. 87',
  stem:'$x(t)=\\cos(2\\pi t)$ is sampled with $T=2/3$ s and reconstructed by an ideal lowpass filter of gain $T$ and cutoff $\\omega_c=\\omega_s/2$. What is $x_r(t)$?',
  opts:['$x_r(t)=\\cos(\\pi t)$','$x_r(t)=\\cos(2\\pi t)$','$x_r(t)=\\cos(3\\pi t)$','$x_r(t)=\\cos(4\\pi t)$'],
  a:0,
  why:'$\\omega_s=2\\pi/T=3\\pi$ rad/s, and $2\\omega_M=4\\pi$, so $3\\pi<4\\pi$ and the copies overlap. With $\\omega_c=1.5\\pi$, the baseband line at $2\\pi$ is outside the filter and is discarded, while the line the $k=1$ copy puts at $\\omega_s-\\omega_0=3\\pi-2\\pi=\\pi$ is inside and is kept.',
  wrong:{1:'That is the answer when the rate is high enough, and here it is not: $\\omega_s=3\\pi$ is below the Nyquist rate $4\\pi$. The line at $2\\pi$ lies outside the cutoff $1.5\\pi$, so the filter removes exactly the component the signal started with.',
         2:'$3\\pi$ is the sampling rate, not a surviving line. The lines sit at $k\\omega_s\\pm\\omega_0$, that is at $\\pm2\\pi$, $\\pi$, $5\\pi$ and so on, and $3\\pi$ is not among them.',
         3:'$4\\pi$ is the Nyquist rate of this signal, not a frequency present after sampling. It is also above the cutoff $1.5\\pi$, so it could not survive the filter in any case.'},
  hints:['Compute $\\omega_s$ and compare it with $2\\omega_M$ for this signal, which is $4\\pi$ rad/s.',
         'List the positions $k\\omega_s\\pm\\omega_0$ and keep only those with magnitude below $\\omega_c$.'],
  sol:'Rate and verdict:$$\\omega_s=\\frac{2\\pi}{T}=\\frac{2\\pi}{2/3}=3\\pi\\ \\text{rad/s},\\qquad 2\\omega_M=4\\pi\\ \\text{rad/s},\\qquad 3\\pi<4\\pi,$$so the copies overlap and aliasing is expected. The cutoff is $\\omega_c=\\omega_s/2=1.5\\pi$ rad/s.<br>Positions of the lines after sampling are $k\\omega_s\\pm\\omega_0$ with $\\omega_0=2\\pi$. Taking $k=0$ gives $\\pm2\\pi$; taking $k=1$ gives $3\\pi-2\\pi=\\pi$ and $3\\pi+2\\pi=5\\pi$; taking $k=-1$ gives $-\\pi$ and $-5\\pi$.<br>Keep only the ones with magnitude below $\\omega_c=1.5\\pi$: that leaves $\\pm\\pi$ and nothing else. So$$X_r(j\\omega)=\\pi\\delta(\\omega-\\pi)+\\pi\\delta(\\omega+\\pi)\\quad\\Longrightarrow\\quad x_r(t)=\\cos(\\pi t).$$Check on the samples: $\\cos(2\\pi nT)=\\cos(4\\pi n/3)$ and $\\cos(\\pi nT)=\\cos(2\\pi n/3)$. Since $4\\pi n/3=2\\pi n-2\\pi n/3$ and the cosine is even, the two sequences are identical, so no reconstruction could tell them apart.<br>Result: $x_r(t)=\\cos(\\pi t)$.',
  err:'Comparing the rate with the wrong bandwidth, or assuming that whatever went in comes out because the filter is called a reconstruction filter.',
  teach:'Ask for the list of line positions before the answer. Once the list is written, the filter step is mechanical.' },

{ id:'Q7-07', module:'M7', kind:'misconception', src:'p. 86',
  stem:'A student writes: "When $\\omega_s<2\\omega_M$, $X(j\\omega)$ is no longer replicated in $X_p(j\\omega)$, so $x(t)$ cannot be recovered." What is wrong with this?',
  opts:['The replication is unconditional and happens at every rate; what changes below $2\\omega_M$ is that neighbouring copies overlap and add. The conclusion is right, and the reason given for it is wrong.',
        'Nothing is wrong: below the Nyquist rate the sum for $X_p$ no longer converges, so the copies genuinely disappear.',
        'The conclusion is wrong. Below the Nyquist rate the copies overlap, but a filter with a narrow enough cutoff can still separate them.',
        'The inequality is the wrong way round: replication stops when $\\omega_s>2\\omega_M$, because the copies then move out of the drawn range.'],
  a:0,
  why:'$X_p(j\\omega)=\\frac{1}{T}\\sum_kX(j(\\omega-k\\omega_s))$ carries no condition on $T$, so a copy sits at every multiple of $\\omega_s$ whatever the rate. Below the Nyquist rate those copies reach each other and add, which is aliasing. The signal is indeed unrecoverable, but because of the overlap, not because the copies stopped appearing.',
  wrong:{1:'The sum is the same sum at every rate. Nothing in it depends on whether the copies overlap; the copies are shifted versions of a fixed function and they are always there. What overlapping changes is the value of the sum inside the overlap, not the number of terms in it.',
         2:'No cutoff helps. Inside the overlap the sampler has stored a single number that is the sum of two contributions, and a filter can only scale values at each frequency. It cannot separate two numbers that were added before it saw them.',
         3:'The copies do not move out of any range; the drawn range is a choice. Above the Nyquist rate the copies are further apart and taller, which is what makes them separable, and they are just as present as before.'},
  hints:['Look at the formula for $X_p(j\\omega)$ and ask which of its symbols is restricted by a condition.',
         'Say in one sentence what sampling always does, and in another what happens only when the rate is too low.'],
  sol:'The statement confuses the operation with its failure mode.<br><b>What is always true.</b>$$X_p(j\\omega)=\\frac{1}{T}\\sum_{k=-\\infty}^{\\infty}X\\bigl(j(\\omega-k\\omega_s)\\bigr)$$holds for every $T>0$. A copy of $X(j\\omega)$ sits at every multiple of $\\omega_s$, scaled by $1/T$. Nothing here refers to $\\omega_M$.<br><b>What depends on the rate.</b> The baseband occupies $|\\omega|\\le\\omega_M$ and the copy at $k=1$ occupies $|\\omega-\\omega_s|\\le\\omega_M$, so the gap between them is $\\omega_s-2\\omega_M$. Positive: the copies stand clear and a filter can cut one out intact. Zero: they touch. Negative: they interpenetrate and the sampler stores their sum.<br><b>Why the conclusion still holds.</b> Inside the overlap $X_p$ carries $\\frac{1}{T}[X(j\\omega)+X(j(\\omega-\\omega_s))]$, a single value made from two. Filtering selects intervals of $\\omega$; it cannot undo an addition. So $x(t)$ is not recoverable — because of the overlap.<br>Result: replication is what sampling does; aliasing is the overlap of what sampling made.',
  err:'Using "replication" and "aliasing" as two names for the same event, which then makes the guard band $\\omega_s-2\\omega_M$ look like a detail instead of the quantity that decides the outcome.',
  teach:'Draw the copies at three rates without changing how many are drawn. The picture makes the distinction before any words do.' },

{ id:'Q7-08', module:'M7', kind:'misconception', src:'p. 82',
  stem:'$x(t)=1+\\cos(2000\\pi t)+\\sin(4000\\pi t)$ is sampled at exactly $\\omega_s=2\\omega_M=8000\\pi$ rad/s. A student concludes that the rate is sufficient, since it meets the Nyquist rate. What actually happens?',
  opts:['The sine term is lost completely: at $\\omega=\\pm4000\\pi$ the baseband and the neighbouring copy contribute equal and opposite weights, and every sample of $\\sin(4000\\pi t)$ is zero.',
        'The rate is sufficient. The theorem requires $\\omega_s\\ge2\\omega_M$, and equality is the efficient choice.',
        'The sine term is recovered but with half its amplitude, because only one of the two contributions at $\\pm4000\\pi$ survives the filter.',
        'The constant term is lost, because the copies meet at $\\omega=0$ when $\\omega_s=2\\omega_M$.'],
  a:0,
  why:'At $\\omega=+4000\\pi$ the baseband contributes $\\frac{1}{T}\\cdot\\frac{\\pi}{j}=+\\frac{4000\\pi}{j}$ and the $k=1$ copy brings $\\frac{1}{T}\\cdot(-\\frac{\\pi}{j})=-\\frac{4000\\pi}{j}$ from $-4000\\pi$. They sum to zero. In time, $\\sin(4000\\pi\\,nT)=\\sin(\\pi n)=0$ for every integer $n$.',
  wrong:{1:'The inequality is strict. With $\\omega_s=2\\omega_M$ the admissible cutoff interval $\\omega_M<\\omega_c<\\omega_s-\\omega_M$ becomes $\\omega_M<\\omega_c<\\omega_M$, which is empty, so the theorem names no filter that could do the job.',
         2:'Half the amplitude would be the outcome of losing one of two equal contributions. Here the two contributions are equal in size and opposite in sign, so what is lost is not half of the term but all of it. The all-zero sample sequence is the direct confirmation.',
         3:'The constant sits at $\\omega=0$ and the nearest copies of it are at $\\pm\\omega_s=\\pm8000\\pi$, far from the origin. It is the band edge at $\\pm\\omega_M$ where the copies meet, not the origin.'},
  hints:['Write the transform of the sine term and identify which copy contributes at $+\\omega_M$ besides the baseband.',
         'Then compute $\\sin(4000\\pi\\,nT)$ with $T=1/4000$ s for a few integers $n$.'],
  sol:'Bandwidth and period: $\\omega_M=4000\\pi$ rad/s, $\\omega_s=8000\\pi$ rad/s, $T=2\\pi/\\omega_s=1/4000$ s $=0.25$ ms.<br><b>Frequency domain.</b> The sine term transforms to $\\frac{\\pi}{j}[\\delta(\\omega-4000\\pi)-\\delta(\\omega+4000\\pi)]$. At the single frequency $\\omega=+4000\\pi$, two terms of the sum for $X_p$ contribute: the baseband, $\\frac{1}{T}\\cdot\\frac{\\pi}{j}=+\\frac{4000\\pi}{j}$, and the $k=1$ copy, which carries the impulse from $-4000\\pi$ up by $\\omega_s$, contributing $\\frac{1}{T}\\cdot(-\\frac{\\pi}{j})=-\\frac{4000\\pi}{j}$. Their sum is exactly zero, and the same happens at $-4000\\pi$.<br><b>Time domain.</b>$$\\sin(4000\\pi\\,nT)=\\sin\\!\\left(4000\\pi\\cdot\\frac{n}{4000}\\right)=\\sin(\\pi n)=0\\quad\\text{for every integer }n.$$Every sample of the sine term is taken at a zero crossing, so the sampler never sees it.<br><b>The repair.</b> Add a guard band. With $\\omega_g=1000\\pi$ rad/s the rate becomes $\\omega_s=9000\\pi$ rad/s, so $T=1/4500$ s, and the admissible cutoff interval $4000\\pi<\\omega_c<5000\\pi$ is no longer empty.<br>Result: at exactly the Nyquist rate the component at the band edge is annihilated, not merely marginal.',
  err:'Reading the theorem with $\\ge$ in place of $>$, which promises a reconstruction filter that the same sentence then rules out.',
  teach:'The empty interval is the fastest argument and needs no example. Give the example afterwards, so the student sees that the empty interval had a signal behind it.' },

{ id:'Q7-09', module:'M7', kind:'exam', src:'p. 82',
  stem:'$x(t)=1+\\cos(2000\\pi t)+\\sin(4000\\pi t)$ is to be sampled with a guard band of $\\omega_g=1000\\pi$ rad/s, that is with $\\omega_s=2\\omega_M+\\omega_g$. Give the sampling period and one admissible reconstruction cutoff.',
  opts:['$T=1/4500$ s, about $222.2\\ \\mu$s, with any cutoff in $4000\\pi<\\omega_c<5000\\pi$ rad/s.',
        '$T=1/4000$ s, that is $0.25$ ms, with any cutoff in $4000\\pi<\\omega_c<5000\\pi$ rad/s.',
        '$T=1/9000$ s, about $111.1\\ \\mu$s, with any cutoff in $4000\\pi<\\omega_c<5000\\pi$ rad/s.',
        '$T=1/4500$ s, about $222.2\\ \\mu$s, with any cutoff in $0<\\omega_c<4000\\pi$ rad/s.'],
  a:0,
  why:'$\\omega_M=4000\\pi$, so $\\omega_s=8000\\pi+1000\\pi=9000\\pi$ rad/s and $T=2\\pi/\\omega_s=2/9000=1/4500$ s. The cutoff must satisfy $\\omega_M<\\omega_c<\\omega_s-\\omega_M$, that is $4000\\pi<\\omega_c<5000\\pi$ rad/s.',
  wrong:{1:'That period belongs to $\\omega_s=8000\\pi$, the Nyquist rate itself, with no guard band at all. At that rate the stated cutoff interval would be empty, so the answer contradicts its own second half.',
         2:'This is $1/\\omega_s$ read as a period without the $2\\pi$: $1/(9000\\pi)$ would be smaller still, and $1/9000$ s corresponds to $\\omega_s=18000\\pi$ rad/s, twice the intended rate.',
         3:'The period is right and the cutoff interval is not. A cutoff below $\\omega_M=4000\\pi$ removes the band edge of the signal itself, so the reconstruction would lose the sine term even though the rate was chosen to preserve it.'},
  hints:['Find $\\omega_M$ from the highest frequency present, then build $\\omega_s$ from the requested guard band.',
         'The cutoff condition is $\\omega_M<\\omega_c<\\omega_s-\\omega_M$; substitute both ends before choosing a value.'],
  sol:'<b>Bandwidth.</b> The three terms sit at $0$, $2000\\pi$ and $4000\\pi$ rad/s, so $\\omega_M=4000\\pi$ rad/s and the Nyquist rate is $2\\omega_M=8000\\pi$ rad/s.<br><b>Rate.</b>$$\\omega_s=2\\omega_M+\\omega_g=8000\\pi+1000\\pi=9000\\pi\\ \\text{rad/s}.$$<b>Period.</b>$$T=\\frac{2\\pi}{\\omega_s}=\\frac{2\\pi}{9000\\pi}=\\frac{2}{9000}=\\frac{1}{4500}\\ \\text{s}\\approx222.2\\ \\mu\\text{s},$$and in hertz $f_s=1/T=4500$ Hz.<br><b>Cutoff.</b>$$\\omega_M<\\omega_c<\\omega_s-\\omega_M\\;\\Longrightarrow\\;4000\\pi<\\omega_c<5000\\pi\\ \\text{rad/s},$$so $\\omega_c=4500\\pi$ rad/s sits in the middle of the interval and is the natural choice.<br><b>Checks.</b> $\\omega_sT=9000\\pi\\times1/4500=2\\pi$. The guard band is $\\omega_s-2\\omega_M=1000\\pi$ rad/s, as requested. And the samples of the sine term are $\\sin(4000\\pi n/4500)=\\sin(8\\pi n/9)$, which is not identically zero, so the component that vanished at the Nyquist rate now survives.',
  err:'Choosing the rate correctly and then placing the cutoff below $\\omega_M$, which throws away the very component the guard band was added to protect.',
  teach:'Require both numbers in the answer. A rate without an admissible cutoff is half a design, and the half that is missing is the one the boundary case breaks.' },

{ id:'Q7-10', module:'M7', kind:'exam', src:'p. 87',
  stem:'$x(t)=\\cos(\\pi t)+\\cos(3\\pi t)$ is sampled with $T=2/5$ s and reconstructed with an ideal lowpass filter of gain $T$ and cutoff $\\omega_c=\\omega_s/2$. What is $x_r(t)$?',
  opts:['$x_r(t)=\\cos(\\pi t)+\\cos(2\\pi t)$','$x_r(t)=\\cos(\\pi t)+\\cos(3\\pi t)$','$x_r(t)=\\cos(\\pi t)$','$x_r(t)=\\cos(2\\pi t)+\\cos(3\\pi t)$'],
  a:0,
  why:'$\\omega_s=2\\pi/T=5\\pi$ rad/s and $\\omega_c=2.5\\pi$. The baseband line at $\\pi$ is inside and survives; the one at $3\\pi$ is outside and is discarded. From the $k=1$ copy, $\\omega_s-3\\pi=2\\pi$ is inside and survives, while $\\omega_s-\\pi=4\\pi$ is outside. So one component comes through unchanged and the other returns at $2\\pi$.',
  wrong:{1:'That would need $\\omega_s>2\\omega_M=6\\pi$ rad/s. Here $\\omega_s=5\\pi<6\\pi$, so the copies overlap and the component at $3\\pi$ does not survive at its own frequency.',
         2:'The low component is right, but the answer stops too soon. The $k=1$ copy puts a line at $\\omega_s-3\\pi=2\\pi$, which is inside the cutoff $2.5\\pi$ and therefore appears at the output. Aliasing adds a component; it does not only remove one.',
         3:'The line at $3\\pi$ lies outside $\\omega_c=2.5\\pi$ and is removed by the filter, so it cannot appear in $x_r$. The component at $\\pi$ is inside and cannot be missing from it.'},
  hints:['Compute $\\omega_s$ and $\\omega_c$, then compare $\\omega_s$ with the Nyquist rate of this signal, which is $6\\pi$ rad/s.',
         'List every line at $k\\omega_s\\pm\\omega_0$ for both components and mark the ones inside $|\\omega|<\\omega_c$.'],
  sol:'<b>Rate.</b> $\\omega_s=2\\pi/T=2\\pi/(2/5)=5\\pi$ rad/s, $\\omega_c=\\omega_s/2=2.5\\pi$ rad/s. The signal has $\\omega_M=3\\pi$, so its Nyquist rate is $6\\pi$ rad/s and $5\\pi<6\\pi$: aliasing is expected.<br><b>Lines inside the filter.</b><br>From the component at $\\pi$: baseband $\\pi$ is inside; the copy line at $5\\pi-\\pi=4\\pi$ is outside.<br>From the component at $3\\pi$: baseband $3\\pi$ is outside; the copy line at $5\\pi-3\\pi=2\\pi$ is inside.<br><b>Recovered spectrum.</b>$$X_r(j\\omega)=\\pi\\bigl[\\delta(\\omega-\\pi)+\\delta(\\omega+\\pi)\\bigr]+\\pi\\bigl[\\delta(\\omega-2\\pi)+\\delta(\\omega+2\\pi)\\bigr]$$and the inverse transform gives$$x_r(t)=\\cos(\\pi t)+\\cos(2\\pi t).$$<b>Check on the samples.</b> $\\cos(3\\pi nT)=\\cos(6\\pi n/5)$ and $\\cos(2\\pi nT)=\\cos(4\\pi n/5)$. Since $6\\pi n/5=2\\pi n-4\\pi n/5$ and the cosine is even, the two sequences are identical, so the sampler cannot distinguish them.<br>Result: one component passes untouched and the other changes identity from $3\\pi$ to $2\\pi$ rad/s.',
  err:'Reporting the whole signal as aliased, or reporting only the loss at $3\\pi$ and not the arrival at $2\\pi$.',
  teach:'Aliasing is component by component. Ask which component moved and where to, rather than whether the signal aliased.' },

{ id:'Q7-11', module:'M7', kind:'graph', src:'p. 81',
  stem:'The figure shows $X_p(j\\omega)$ for a signal with $\\omega_M=2\\pi$ rad/s. Reading the figure, which statement is correct?',
  figure:()=>{ const PI=Math.PI, ws=5*PI, pk=2.5;
    const tri=(w,wm,p)=>Math.abs(w)<=wm?p*(1-Math.abs(w)/wm):NaN;
    const a=PLOT.Axes({w:820,h:230,xr:[-8*PI,8*PI],yr:[-0.25,3.4],
      xlabel:'\\omega\\;[\\text{rad/s}]',ylabel:'X_p(j\\omega)',
      pad:{l:58,r:26,t:28,b:36},ytarget:3,
      xticksOverride:[-5*PI,-2*PI,0,2*PI,5*PI],
      xtickfmt:v=>{const r=Math.round(v/PI);return r===0?'0':(r<0?'-':'')+Math.abs(r)+'π';}});
    for(let k=-2;k<=2;k++) a.curve(w=>tri(w-k*ws,2*PI,pk),{color:k===0?PLOT.COL.in:(Math.abs(k)===1?PLOT.COL.mid:PLOT.COL.slate),n:1400});
    a.span(2*PI,ws-2*PI,2.9,'\\text{this gap}',{color:PLOT.COL.out,fs:13,tex:true});
    return a.svg(); },
  opts:['The centre group is the baseband, the sampling rate is $5\\pi$ rad/s, and the marked gap is the guard band $\\omega_s-2\\omega_M=\\pi$ rad/s.',
        'The centre group is the first replica, the sampling rate is $5\\pi$ rad/s, and the marked gap is $\\omega_s-\\omega_M=3\\pi$ rad/s.',
        'The sampling rate is $2\\pi$ rad/s, read off the point where the centre group reaches zero, and the marked gap is $3\\pi$ rad/s.',
        'The copies overlap, because the marked gap lies between two of them, and the signal cannot be recovered.'],
  a:0,
  why:'The copies are centred at multiples of $\\omega_s$, so the spacing between centres gives $\\omega_s=5\\pi$ rad/s. The group at the origin is the signal\u2019s own spectrum, the baseband, numbered $k=0$. The gap runs from the top of the baseband at $\\omega_M=2\\pi$ to the bottom of the next copy at $\\omega_s-\\omega_M=3\\pi$, so it is $\\pi$ rad/s wide, which is $\\omega_s-2\\omega_M$.',
  wrong:{1:'The numbering is off by one and the width is misread. The centre group is not a copy of anything; it is $X(j\\omega)/T$ where it always sat. And the gap ends at $\\omega_s-\\omega_M=3\\pi$ but begins at $\\omega_M=2\\pi$, not at the origin, so its width is $\\pi$.',
         2:'$2\\pi$ is where the baseband reaches zero, so it is $\\omega_M$, not the rate. The rate is the distance between the centres of neighbouring copies, which is $5\\pi$ rad/s. The height of each copy, $1/T=2.5$, agrees with that reading.',
         3:'A gap is the opposite of an overlap. Its presence is what says the copies stand clear of one another, and a filter with a cutoff anywhere inside it recovers the signal exactly.'},
  hints:['The rate is the distance between the centres of neighbouring copies, not the width of one of them.',
         'The gap begins at the top of the baseband and ends at the bottom of the next copy. Write both endpoints before subtracting.'],
  sol:'<b>Reading the rate.</b> The copies are centred at $0,\\pm\\omega_s,\\pm2\\omega_s,\\dots$, and the figure shows centres at $0$, $\\pm5\\pi$ and $\\pm10\\pi$, so $\\omega_s=5\\pi$ rad/s. The period follows: $T=2\\pi/\\omega_s=0.4$ s, and the height of each copy should then be $1/T=2.5$, which is what the vertical axis shows.<br><b>Naming the groups.</b> The term $k=0$ of the sum is $X(j\\omega)/T$, the spectrum of the signal itself. It is the baseband, and it is not a copy. The groups at $\\pm5\\pi$ are $k=\\pm1$, the first copies.<br><b>The gap.</b> The baseband occupies $|\\omega|\\le\\omega_M=2\\pi$. The copy at $k=1$ occupies $|\\omega-5\\pi|\\le2\\pi$, so it begins at $5\\pi-2\\pi=3\\pi$. The gap is therefore$$(\\omega_s-\\omega_M)-\\omega_M=\\omega_s-2\\omega_M=5\\pi-4\\pi=\\pi\\ \\text{rad/s}.$$<b>Verdict.</b> The gap is positive, so the copies stand clear and the signal is recoverable with any cutoff in $2\\pi<\\omega_c<3\\pi$ rad/s.<br>Result: baseband at the centre, $\\omega_s=5\\pi$ rad/s, guard band $\\pi$ rad/s.',
  err:'Calling the centre group the first replica, which shifts every later index by one, and measuring the gap from the origin instead of from the band edge.',
  teach:'Ask for three numbers off any replica diagram before anything else: the spacing, the height, and the two ends of the gap. All three are readable, and each one checks the others.' },

{ id:'Q7-12', module:'M7', kind:'synthesis', src:'pp. 80–88',
  stem:'A signal of finite duration is to be recorded digitally and played back. Which chain gives an output that is exactly a band-limited version of the input, and why is nothing better available?',
  opts:['Lowpass filter, then sample above twice the filter cutoff, then reconstruct with gain $T$ and a cutoff inside the guard band. A signal of finite duration is never band-limited, so the band limit has to be imposed before sampling rather than assumed.',
        'Sample above twice the highest frequency of interest, then reconstruct with gain $T$. A finite-duration signal is band-limited to that frequency, so no filter is needed before the sampler.',
        'Sample at any rate, then reconstruct with a zero-order hold and raise the rate until the steps are small enough. In the limit the hold output equals the input.',
        'Sample above twice the highest frequency of interest, then reconstruct with gain 1 and a cutoff at $\\omega_s$, so that all the copies are kept and no information is discarded.'],
  a:0,
  why:'A signal that is exactly zero outside a finite interval of time has a transform that keeps crossing zero without ever staying there, so there is no $\\omega_M$ beyond which it vanishes and no rate makes the copies disjoint. The band limit is therefore created by a filter, and what is then reconstructed exactly is the filtered signal.',
  wrong:{1:'Finite duration and finite bandwidth cannot hold together. The transform of a rectangular pulse of width $2T_1$ is $2\\sin(\\omega T_1)/\\omega$, which is non-zero on every interval however far out, so some overlap is present at every rate. Without a filter first, the recording contains content the signal never had.',
         2:'The steps get smaller and the error gets smaller, but neither reaches zero at any finite rate. The staircase has jumps and the signal does not, and the hold response is neither flat inside the band nor zero outside it.',
         3:'Keeping all the copies is not keeping all the information; the copies are repetitions of the same information at other frequencies. A cutoff at $\\omega_s$ admits both neighbours, and gain 1 leaves the result a factor $T$ too small.'},
  hints:['Ask whether a signal can be exactly zero outside a finite time interval and also exactly zero outside a finite band.',
         'Then decide whether the missing band limit should be imposed before or after the sampler.'],
  sol:'<b>The obstacle.</b> Take the recording window as a rectangular pulse of width $2T_1$. Its transform is $2\\sin(\\omega T_1)/\\omega$, which crosses zero at every multiple of $\\pi/T_1$ but is never zero on a whole interval. So there is no finite $\\omega_M$ with $X(j\\omega)=0$ beyond it, and for every rate the tails of neighbouring copies add. Some aliasing is present at every rate.<br><b>The chain.</b><br>1. An anti-aliasing filter with cutoff $\\omega_1$ imposes the band limit. Everything above $\\omega_1$ is discarded here, while it is still at its own frequency and can be discarded cleanly.<br>2. Sampling at $\\omega_s>2\\omega_1$ leaves a guard band $\\omega_s-2\\omega_1>0$, so the copies stand clear.<br>3. Reconstruction with gain $T$ and cutoff $\\omega_c$ strictly inside $\\omega_1<\\omega_c<\\omega_s-\\omega_1$ returns the filtered signal exactly:$$X_r(j\\omega)=T\\cdot\\frac{1}{T}X_1(j\\omega)=X_1(j\\omega).$$<b>What is being claimed.</b> The output equals the filtered signal $x_1(t)$, not the original $x(t)$. That difference is a design decision with a known size, set by the choice of $\\omega_1$.<br><b>Why nothing better exists.</b> Without step 1 the error is not a known loss of high frequencies but the arrival of content at frequencies the signal never contained, and nothing downstream can distinguish that content from the signal.<br>Result: an exactly reconstructible chain that is honest about what it discards.',
  err:'Assuming band-limitedness because the interesting content happens to be low, and so leaving the anti-aliasing filter out of the design entirely.',
  teach:'This question closes the module. Ask for the chain to be drawn with every stage named, and for one sentence saying what the output is equal to. The sentence is the part that separates a design from a diagram.' }

]);
